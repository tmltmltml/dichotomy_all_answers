#include <cmath>                                     //mathematica lib
//#include <iostream>                                  //standard input and output lib
double fun(double x)
{
	double y = 4 * x * x * x - 8 * x * x + 2 * x + 1;
	return y;
}

//eg:
//C++              math
//exp(x)           e ^ x;
//pow(x, *)        x ^ *;
//cos(x)           cosx;
//sin(x)           sinx;
//acos(x)          arccosx;
//asin(x)          arcsinx;
//atan(x)          arctanx;
//fabs(x)          |x|;
//log(x)           lnx;
//log10(x)         lgx;
//sqrt(x)          x ^ (1 / 2);
